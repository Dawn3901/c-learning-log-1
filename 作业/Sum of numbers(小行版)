#include<iostream>
#include<string>

using namespace std;

int main()
{
	while (1)
	{
		string n;
		cin >> n;
		if (n == "0") return 0;
		else {
			int sum = 0;
			for (auto i : n) {
				sum += static_cast<int>(i - '0');
			}
			cout << sum << endl;
		}
	}

}
